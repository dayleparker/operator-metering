apiVersion: metering.openshift.io/v1
kind: MeteringConfig
metadata:
  name: "operator-metering"
spec:
  storage:
    type: "hive"
    hive:
      type: "azure"
      azure:
        container: "bucket1"
        storageAccountName: "meteringazure"
        secretAccessKey: "secret-key-here"
        createSecret: true
        secretName: ""
        rootDirectory: "/testDir"